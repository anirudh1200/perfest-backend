version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.9
    steps:
      - checkout
      - deploy:
          name: Deploy to Heroku
          command: git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git develop
